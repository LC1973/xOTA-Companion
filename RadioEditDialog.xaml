<Window x:Class="xOTACompanion.RadioEditDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:local="clr-namespace:xOTACompanion"
        xmlns:wc="clr-namespace:System.Windows.Shell;assembly=PresentationFramework"
        Title="Radio"
        Width="420" SizeToContent="Height"
        WindowStartupLocation="CenterOwner"
        WindowStyle="None" ResizeMode="NoResize"
        Background="{StaticResource DarkPanel}"
        BorderThickness="1" BorderBrush="{StaticResource DarkBorder}"
        AllowsTransparency="True">
    <wc:WindowChrome.WindowChrome>
        <wc:WindowChrome CaptionHeight="36" GlassFrameThickness="0" CornerRadius="0" UseAeroCaptionButtons="False"/>
    </wc:WindowChrome.WindowChrome>
    <StackPanel>
        <Border Background="#FF2D2D30" Height="36" MouseLeftButtonDown="TitleBar_MouseDown">
            <TextBlock Text="Edit Radio" Foreground="{StaticResource DarkText}" FontSize="{StaticResource TitleFontSize}"
                       FontWeight="SemiBold" VerticalAlignment="Center" Margin="12,0"/>
        </Border>
        <StackPanel Margin="20,16,20,20" local:SP.Gap="10">
            <StackPanel local:SP.Gap="4">
                <TextBlock Text="Friendly name *" Foreground="{StaticResource DarkTextSecondary}" FontSize="10" FontWeight="Bold"/>
                <TextBox x:Name="NameBox" Style="{StaticResource DarkTextBox}"/>
            </StackPanel>
            <StackPanel local:SP.Gap="4">
                <TextBlock Text="Control type" Foreground="{StaticResource DarkTextSecondary}" FontSize="10" FontWeight="Bold"/>
                <ComboBox x:Name="TypeCombo" Style="{StaticResource DarkComboBox}" SelectionChanged="TypeCombo_SelectionChanged">
                    <ComboBoxItem Content="None"/>
                    <ComboBoxItem Content="CAT"/>
                    <ComboBoxItem Content="TCI"/>
                    <ComboBoxItem Content="CI-V (ICOM)"/>
                </ComboBox>
            </StackPanel>

            <!-- CAT panel -->
            <StackPanel x:Name="CatPanel" local:SP.Gap="8">
                <Separator Background="{StaticResource DataGridGridLines}"/>
                <TextBlock Text="CAT SETTINGS" Foreground="{StaticResource DarkTextSecondary}" FontSize="10" FontWeight="Bold"/>
                <Grid>
                    <Grid.ColumnDefinitions><ColumnDefinition Width="*"/><ColumnDefinition Width="12"/><ColumnDefinition Width="*"/></Grid.ColumnDefinitions>
                    <StackPanel local:SP.Gap="4">
                        <TextBlock Text="COM Port" Foreground="{StaticResource DarkTextSecondary}" FontSize="10" FontWeight="Bold"/>
                        <ComboBox x:Name="CatPortCombo" Style="{StaticResource DarkComboBox}"/>
                    </StackPanel>
                    <StackPanel Grid.Column="2" local:SP.Gap="4">
                        <TextBlock Text="Baud Rate" Foreground="{StaticResource DarkTextSecondary}" FontSize="10" FontWeight="Bold"/>
                        <ComboBox x:Name="BaudCombo" Style="{StaticResource DarkComboBox}"/>
                    </StackPanel>
                </Grid>
            </StackPanel>

            <!-- CI-V extra settings (address) – shown together with CatPanel when CI-V is selected -->
            <StackPanel x:Name="CivPanel" local:SP.Gap="4" Visibility="Collapsed">
                <Grid>
                    <Grid.ColumnDefinitions><ColumnDefinition Width="Auto"/><ColumnDefinition Width="8"/><ColumnDefinition Width="80"/></Grid.ColumnDefinitions>
                    <TextBlock Text="CI-V Address (hex)" Foreground="{StaticResource DarkTextSecondary}" FontSize="10" FontWeight="Bold" VerticalAlignment="Center"/>
                    <TextBox x:Name="CivAddressBox" Grid.Column="2" Style="{StaticResource DarkTextBox}" Text="94"
                             ToolTip="Hexadecimal CI-V address shown in the radio&#x0a;menu (e.g. 94 for IC-7300, A4 for IC-705)."/>
                </Grid>
            </StackPanel>

            <!-- TCI panel -->
            <StackPanel x:Name="TciPanel" local:SP.Gap="8" Visibility="Collapsed">
                <Separator Background="{StaticResource DataGridGridLines}"/>
                <TextBlock Text="TCI SETTINGS" Foreground="{StaticResource DarkTextSecondary}" FontSize="10" FontWeight="Bold"/>
                <Grid>
                    <Grid.ColumnDefinitions><ColumnDefinition Width="*"/><ColumnDefinition Width="12"/><ColumnDefinition Width="80"/></Grid.ColumnDefinitions>
                    <StackPanel local:SP.Gap="4">
                        <TextBlock Text="Host / IP" Foreground="{StaticResource DarkTextSecondary}" FontSize="10" FontWeight="Bold"/>
                        <TextBox x:Name="TciHostBox" Style="{StaticResource DarkTextBox}" Text="127.0.0.1"/>
                    </StackPanel>
                    <StackPanel Grid.Column="2" local:SP.Gap="4">
                        <TextBlock Text="Port" Foreground="{StaticResource DarkTextSecondary}" FontSize="10" FontWeight="Bold"/>
                        <TextBox x:Name="TciPortBox" Style="{StaticResource DarkTextBox}" Text="40001"/>
                    </StackPanel>
                </Grid>
            </StackPanel>

            <CheckBox x:Name="IsDefaultCheck" Content="Use as default radio" Style="{StaticResource DarkCheckBox}"/>
            <Separator Background="{StaticResource DataGridGridLines}"/>
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" local:SP.Gap="8">
                <Button Content="Cancel" Style="{StaticResource SecondaryButton}" Width="80" Click="Cancel_Click"/>
                <Button Content="Save" Style="{StaticResource DarkButton}" Width="80" IsDefault="True" Click="Save_Click"/>
            </StackPanel>
        </StackPanel>
    </StackPanel>
</Window>
